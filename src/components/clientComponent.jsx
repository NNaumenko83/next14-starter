'use client';

import React from 'react';

const ClientComponent = ({ children }) => {
    return (
        <div>
            clientComponent
            {children}
        </div>
    );
};

export default ClientComponent;
